pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // 添加本地仓库
        flatDir {
            dirs 'libs' // 指向项目根目录下的 libs 文件夹
        }
    }
}

rootProject.name = "XYproxyLib"
include ':app'
include ':proxy_lib'
